import React from "react";

export default function MenuCategoryItem({ name, price }) {
  const { name, price } = beverage;
  return (
    <li>
      <div>{name}</div>
      <div>{price}</div>
    </li>
  );
}
